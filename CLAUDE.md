# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 我的语文小屋 — 项目准则

面向小学生的语文练习站。姊妹项目 `../english`（英语小屋）——**结构、工程约定、发布流程一律照它来**，
遇到本文件没写到的问题，先看 `../english/CLAUDE.md` 怎么定的。

纯静态 HTML + Python 生成脚本，无构建步骤、无依赖安装（`pypinyin` 是可选增强，缺了也能跑）。

## 栏目隔离
- **每个栏目整体隔离在自己的目录**（今日练习 `practice/`、朗读打卡 `recite/`、抽查单 `check/`、
  要求总表 `outline/`）；根目录只放站点级文件
  （栏目导航页 `index.html`、`Makefile`、`.nojekyll`），栏目之间不互相引用。
- 栏目内部全部相对路径，无论部署在根域名还是 `/仓库名/` 子路径下都能打开。
- 新栏目另建独立目录，并在根 `index.html` 加一张卡片（未上线的标 `soon`）。

## practice/ — 今日练习单
「看拼音写汉字」A4 打印单：拼音在上，田字格在下。`specs/<YYYYMMDD>.txt` → `sheets/<YYYYMMDD>.html|.pdf`。
spec 完整语法见 `practice/README.md`；日常工作流见 `/practice` skill。

- **标拼音是这个栏目的核心职责，也是唯一容易出错的地方**。脚本只排版，不判断读音：
  多音字必须在 spec 里手写，按**课文语境**定音——`单=dān`（不是 shàn）、`悄悄=qiāo qiāo`（不是 qiǎo）。
  装了 `pypinyin` 的自动补全只对单音字可信，多音字一律手写覆盖。
- **音节数必须等于字数**（`麻雀=má què` → 2 格），不符时脚本报错并提示写法。
- 生字比词语更需要练手，惯例 `[生字] copies=3`、`[词语] copies=2`；
  `copies` / `cell` 是**排满一页 A4** 的两个旋钮，别让它溢出到第二页、也别空掉小半页。
- 答案版（`--answers`）格子里印楷体汉字，给家长批改用，**不进目录页**。
- 田字格用 CSS 画（实线外框 + 十字虚线），所以 `print-color-adjust: exact` 必须留着，
  否则打印时红线全没了；相邻格子靠负边距共享边线，别改成各画一圈边框（会叠成粗线）。

## recite/ — 朗读打卡单
要背的课文 A4 打印单：大字正文 + 底部一排打卡圈，**一篇课文一页**。
`specs/<YYYYMMDD>.txt` → `sheets/<YYYYMMDD>.html|.pdf`。spec 完整语法见 `recite/README.md`。

- **课文原文一字不改**，标点、省略号、分段照课本抄；拿不准是哪一段就问，别凭印象补。
- 注音写在正文里（`鼎(dǐng)沸(fèi)`），**拼音只跟前面那一个汉字**，两个字就写两遍。
  只标难字和多音字（`少(shào)女`、`衣裳(shang)`），满篇注音反而没法读。
- **正文字号全篇统一**（文件头 `size: 27`，别逐篇 `size=`）：朗读单字大才好读，各篇一致才像一份东西。
  27px 下最长的《观潮》3-4 段正好一页，更长的课文才调小；短课文下半页留白是正常的。
- **页头不印固定日期**（`date:` 留空）——同一份要读好几天，日期让孩子填页头右边那栏。
- 打卡圈钉在页底，靠 `@media print` 里的 `.page{height:273mm}` 撑高页面，别删。
- 文言文用 `note:` 附一句白话意思；白话课文一般不用。

## check/ — 抽查单
学完一课后家长照着问一遍，逐项打勾。**一课一张，一张一页**：`specs/<课号>.txt` →
`sheets/<课号>.html|.pdf`（`--answers` 出家长版）。spec 完整语法见 `check/README.md`。

- **spec 按课号命名**（`01.txt` = 第 1 课），不按日期——学完哪课打哪张，
  几课混在一张上就看不出是哪课没记住。目录页按课号升序。
- **两版分开**：题面版给孩子（答案不印，动笔的题空着写，页底错题格吃掉空白），
  家长版 `--answers`（答案全印，听写横线压到 1 行）。家长版**不进目录页**，
  和 practice 的答案版一个道理。
- **字号是给孩子看的**：题面楷体 20px、块名 18px，别为了塞内容调小——塞不下就删块。
- **以口头题为主**（读音、背诵、复述）——十分钟才问得完。动笔的题超过两三项就该去 `practice/`
  出正经的默写单，别把抽查单撑成练习单。
- `pass=` 要写得能一眼判定（`错 ≤ 1 个`、`零错字`、`顺序说对`），别写"基本掌握"这种没法判的。
  `pass=` 的值可以带空格，所以要写在属性最后。
- **背诵题的答案写成「关键节点 → 关键节点」的链条，别抄整段原文**——家长扫一眼就知道漏了哪句。
- 一课的常规块序：读准（多音字/易错音）→ 背诵（书上要求的才有）→ 听写（本课写字表抽几个）
  → 课后题（口头）→ 词语意思，5 块 15~20 项。这一课要不要背、要不要默写先查 `outline/` 的总表。
- `specs/20260825.txt` 是改成一课一张之前试做的「跨一~四单元 10 分钟单」，**不是范例**。

## outline/ — 课文要求总表
一册一份的宏观总表：每课一行，哪篇要背、哪首要默写一眼看到。同一份 spec 出 A4 打印单
（`sheets/g4a.html|.pdf`）+ 网页速查页（`index.html`）。spec 完整语法见 `outline/README.md`。

- **要求一律照课后题原话**（"有感情地朗读课文。背诵第 3~4 自然段。"），
  **别发明"熟悉""掌握""熟读成诵"**——书上没有这些词。
- **背诵和默写分两列**：要求背的不一定要默写。1~14 课只有《题西林壁》写了"默写"。
- **一册一份，按册命名不按日期**（`g4a` = 四年级上册），这是和别的栏目唯一不一样的地方。
- **课本没覆盖的单元写 `recite=待补`，不猜、不从别的版本抄**。手上的 2024 版电子课本只到书页 53
  （一~四单元）；2024 版和 2019 版课次编号不同（《走月亮》2024 版已删，第 2 课是《繁星》），
  混用错得很隐蔽。
- 日积月累算"要背"，但和课文背诵分开统计，页头分别显示。

## 内容准则
- **不增不删**：用户给的字词一个不落、也不自己加练习项。字词的**课次分组照抄**，不重排合并。
- 每天的练习单是独立一份 spec；只是换个日期时 `cp specs/<旧>.txt specs/<新>.txt` 再改，别从头写。
- 用户给的内容有歧义（字词数量对不上、看不出是哪一课）先问，别猜。
- 排版细节：中文与数字/英文之间留空格（`16 课`）。

## 工程与发布
- 生成脚本**用相对路径，一律在自己的栏目目录下运行**（`cd practice && python3 generate_practice.py …`）。
- PDF 由无头 Chrome 导出。**改完排版一定 Read 生成的 PDF 自检**，别凭 HTML 源码想象效果。
- 本地预览 `make up`（会打印手机/iPad 同 WiFi 地址 + 二维码）；`make practice` 生成最新一份练习单。
- `practice/index.html` 由脚本自动生成（GitHub Pages 不列目录），**不要手改**——改模板改脚本里的 `INDEX_TEMPLATE`。
- 重大改动 / 体系取舍先和用户讨论；确认后 commit（用户要求才 push）。
- 分支 **master**，远端 `git@github.com:bluebu/chinese.git`，GitHub Pages 地址 `https://bluebu.github.io/chinese/`。
- **自定义域名尚未确定**：还没有 `CNAME`。定下来后在根目录加（参照 `../english` 的 `english.hi-ruby.com`）。
- 本地预览默认端口 **8001**（8000 留给英语站）。
